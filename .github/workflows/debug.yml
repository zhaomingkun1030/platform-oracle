name: Debug Build

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Debug info
        run: |
          echo "=== Files in repo ==="
          ls -la
          echo "=== Docker file ==="
          cat Dockerfile
          echo "=== Backend files ==="
          ls -la backend/
